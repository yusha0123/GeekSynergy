import Icon from "./Icon";

const Navbar = () => {
  return (
    <header className="inset-x-0 sticky top-0 bg-primary px-3 md:px-6 xl:px-10 py-2 md:py-3 shadow-sm z-10">
      <Icon className="w-10 h-10" />
    </header>
  );
};

export default Navbar;
